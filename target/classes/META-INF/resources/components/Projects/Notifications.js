import React from 'react'
import "./Notifications.css"


function Notifications(props) {
  const { notifications } = props;

  return (
    <div className="Notifications_container card">
      These are the notifications
    </div>
  )
}

export default Notifications
